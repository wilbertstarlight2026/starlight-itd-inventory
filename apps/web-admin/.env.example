# Set in Vercel Project > Settings > Environment Variables
# Leave empty to use relative /api proxy (same-domain Vercel deployment)
VITE_API_URL=

# Optional: override base path if hosted on a subpath
# VITE_BASE_PATH=/admin
